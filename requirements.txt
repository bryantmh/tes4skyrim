# Runtime dependencies, pinned to the versions the conversion is tested with.
# Install with: pip install -r requirements.txt. The portable package
# (tools/release/build_portable.py) installs exactly this list.
PyFFI==2.2.3
numpy==2.5.3
scipy==1.18.1
shapely==2.1.2
Pillow==12.3.0
lz4==4.4.5
mapbox_earcut==2.1.0
setuptools==84.0.0
# Optional: drag mod archives onto the GUI.
tkinterdnd2==0.6.3
