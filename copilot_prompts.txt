1) There is a bug with should_skip_dial that causes ALL dialog topics to disappear from the NPC when enabled in import_main. Activating them with the lines enabled gives no response. Fix this. Simply commenting out the places where it is called allows all dialog to appear again when activating an NPC. This has been tested multiple times. This IS A BUG. 2) Speedtree conversion isn't great right now. Comment out the lines running our own conversion for now, and instead, at runtime, dynamically pick the most similar assets/speedtrees tree to the one being replaced and copy/rename it into the proper meshes/tes4/speedtrees location (And don't forget its textures!).

1) Your collision is now rotated from where it is supposed to be, BUT I haven't had a crash walking over that area, so the fix is probably semi-correct. You need to make sure the collision is in the correct orientation. To test, you can convert a mesh with the old code and make sure the collision ends up with the correct / same coordinates when you move it to the root bsfadenode. 2) Environmental sounds play but fail to loop. Investigate this issue and fix. 3) Finally, fix interaction markers. They are incorrectly placed with NPCs sitting in the air and reversed over chairs for example. You should analyze skyrim furniture to find where the markers are supposed to be, and ensure they are added to similar locations on the converted furniture. It may be as simple as doing something similar to armor with the offsets in skyrim_overrides

1) the script converter is big and unwieldy, move the constants to their own file (at least) and move new files to a new script_convert folder. 2) You have placed a few script files in the output folder (TES4Polyfill.psc, etc.). No non-generated file should EVER be placed in the output folder. Move those to the script_convert folder and copy them over to output as part of the script convert process. 3) I have added a functional (though not complete) implementation in the references/ScriptConverter. Analyze its code and implement anything it has that we are missing. I particularly like its better separation of concerns, and it is possible that it is more complete and accurate in some ways (ie deals with things that our scripts currently skip or label as having no equivalent). Make sure to record your findings in a md file and perform a refactor of our converter if the findings necessitate it 4) Make script and compile a single step in convert.py. 5) Continue to iterate on the scripts. Ensure they are properly linked in the esm to things like quests, dialogue, objects, etc, and skip any scripts that are attached to things overridden in skyrim_overrides. Also ensure that ALL (100%) of scripts compile. 6) Iterate on the TODOs in the script. Most should be able to be replaced with a polyfill/custom implementation. I am looking at 90%+ full conversion rate for this session (meaning all todos are gone and the script compiles and runs as intended based on the source). If a system doesn't exist and it's just a value, just add and update a global value (for example fame/infamy) and I can display it onscreen later. This will allow all scripts using it to function as intended. You can look at skyrim source scripts for understanding on what is available. There are also things you have marked as missing in skyrim, such as the player being in jail, but skyrim has a jail / crime system, so you need to make sure everything is hooked in to use that. Skyrim also has systems for merchants, trainers, etc. Again, I am looking at a 90%+ conversion rate here, with the oblivion intent cleanly transferrred over and me able to complete full quests. Iterate until this benchmark is achieved. One think I don't like in particular is this line: --- External references (fill in CK) --- I don't want to have to do anything in the CK. running the esm / script converter everything should just work.