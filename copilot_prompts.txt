1) There is a bug with should_skip_dial that causes ALL dialog topics to disappear from the NPC when enabled in import_main. Activating them with the lines enabled gives no response. Fix this. Simply commenting out the places where it is called allows all dialog to appear again when activating an NPC. This has been tested multiple times. This IS A BUG. 

1) Your collision is now rotated from where it is supposed to be, BUT I haven't had a crash walking over that area, so the fix is probably semi-correct. You need to make sure the collision is in the correct orientation. To test, you can convert a mesh with the old code and make sure the collision ends up with the correct / same coordinates when you move it to the root bsfadenode. The code in question is in nif_converter _convert_nif. I have commented out the code that creates wrong orientations, and left the likely crashing culprit (but correct orientation) code active. To review the TODOS, they are in TODO.txt major issue 10. 2) Environmental sounds play but fail to loop. Investigate this issue and fix. 3) Speedtree conversion isn't great right now. Comment out the lines running our own conversion for now, and instead, create a function that at runtime, dynamically picks the most similar assets/speedtrees tree based on file name to the one being replaced and copy/rename it into the proper meshes/tes4/speedtrees location (And don't forget its textures!). 4) Finally, fix interaction markers. They are incorrectly placed with NPCs sitting in the air and reversed over chairs for example. You should analyze skyrim furniture to find where the markers are supposed to be, and ensure they are added to similar locations on the converted furniture. It may be as simple as doing something similar to armor with the offsets in skyrim_overrides

Iterate on the dialog / quest system. There are many NPCs that recieve dialog options that they shouldn't have, either because they are supposed to belong to another npc, or because the dialog is only supposed to appear in a later quest stage. 1) Create a comprehensive document showing how tes4 dialog / quests are supposed to map to tes5 quests (save in an md file to reference later). 2) Write some extensive tests detailing what output a given input should produce and vice versa. You can attempt to "reverse" convert some skyrim records to oblivion to ensure your process is airtight. Also tests to emulate how Skyrim would handle the records to see what individual NPCs would show for their dialog options. Key indicators are that ONLY the correct dialog should appear on an npc, and I should be able to begin the correct quests at the right time (now that we have script conversion working) and that later dialog won't appear until the proper quest conditions are met. 3) Iterate on your design dialog and quests are in the correct skyrim format and functional while continuously making your documentation more accurate and tests more strict and comprehensive. 4) Also note the problematic --- External references (fill in CK) --- line in the script converter. Any step that "needs" the CK should modify the converted ESM correctly, including for added globals and factions like TES4Infamy or TES4CyrodiilCrimeFaction if a master file doesn't already have them filled out.
